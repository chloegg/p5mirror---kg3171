function setup() {
  createCanvas(800, 500);
}

function draw() {
  background(255, 0, 0); // Should show a solid red background
  console.log("If this shows in the console, JS is loading correctly.");
}

